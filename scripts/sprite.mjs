import fs from'node:fs';import path from'node:path';const src='src/assets/icons',out='dist/assets/sprite.svg';fs.mkdirSync(path.dirname(out),{recursive:true});const files=fs.existsSync(src)?fs.readdirSync(src).filter(x=>x.endsWith('.svg')):[];const symbols=files.map(f=>{const s=fs.readFileSync(path.join(src,f),'utf8').replace(/^[\s\S]*?<svg[^>]*>/i,'').replace(/<\/svg>\s*$/i,'');return `<symbol id="${path.basename(f,'.svg')}">${s}</symbol>`}).join('');fs.writeFileSync(out,`<svg xmlns="http://www.w3.org/2000/svg">${symbols}</svg>`);console.log('Sprite:',files.length);