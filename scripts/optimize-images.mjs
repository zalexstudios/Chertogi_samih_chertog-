import fs from'node:fs';import path from'node:path';import sharp from'sharp';const src='src/assets/images',out='dist/assets/images';fs.mkdirSync(out,{recursive:true});if(!fs.existsSync(src)){console.log('No images');process.exit(0)}for(const f of fs.readdirSync(src)){const i=path.join(src,f),e=path.extname(f).toLowerCase(),b=path.basename(f,e);if(['.jpg','.jpeg','.png'].includes(e)){await sharp(i).webp({quality:82}).toFile(path.join(out,b+'.webp'));await sharp(i).avif({quality:65}).toFile(path.join(out,b+'.avif'))}}console.log('Images optimized');