export function initCounter(){document.querySelectorAll('[data-counter]').forEach(el=>{const target=+el.dataset.counter;let n=0;const go=()=>{n=Math.min(target,n+Math.max(1,Math.ceil(target/50)));el.textContent=n.toLocaleString('ru-RU');if(n<target)requestAnimationFrame(go)};const o=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){go();o.disconnect()}}));o.observe(el)})}