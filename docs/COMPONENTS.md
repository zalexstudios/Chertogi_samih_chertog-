# Components

burger, tabs, spoiler, popup, counter, theme, form validation, running line, parallax, cursor, gallery, masonry, fullpage, cart, page navigation.

Modules are independent and can be imported per page.
